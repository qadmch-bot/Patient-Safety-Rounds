# جدولة تذكيرات الجولات

أوقفنا Cron التلقائي للخطط التصحيحية. إرسال رابط الخطة الأول عند اعتماد الملاحظة باقٍ في api/observations.js. التذكيرات اللاحقة يدويًا من إدارة الجودة.

## تشغيل تذكيري 24 ساعة وساعة

خطة Vercel Hobby لا تدعم تشغيل Cron كل دقيقة. لذلك لا يوجد Cron دوري في vercel.json. لضمان دقة الموعدين استخدمي خدمة جدولة خارجية موثوقة تستدعي GET https://patient-safety-rounds.vercel.app/api/process-reminders?type=round كل دقيقة، مع ترويسة Authorization: Bearer <CRON_SECRET>. عيّني CRON_SECRET في متغيرات بيئة Vercel. لا تستخدمي رابط deployment مؤقت. تأكدي من سماح مزود الجدولة بترويسة Authorization ومن قيود خطة Vercel ومعدل الاستدعاءات.

لن يُرسل المعالج رسائل متأخرة أكثر من 15 دقيقة؛ يجب تشغيل المجدول كل دقيقة لضمان قرب الموعد. لا تفعّلي مجدولًا خارجيًا قبل اختبار الأرقام والقوالب والروابط باستخدام جولة مستقبلية تجريبية.

اضبطي PUBLIC_BASE_URL=https://patient-safety-rounds.vercel.app في إعدادات Vercel. لا تضعي أسرار Twilio أو Supabase داخل المستودع.

ملاحظة: الكود الحالي يطلب إرسال رسائل WhatsApp عند الاعتماد، لكن قبول Meta/Twilio للقالب وتسليم الرسالة الفعلي يحتاجان اختبارًا من لوحة المزود.
